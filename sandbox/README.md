# sandbox

Use this for experiments that are not part of the product yet.

Rules:

- Do not import sandbox code from `src/`.
- Promote useful experiments into `src/` through a normal task.
- Delete stale experiments.
